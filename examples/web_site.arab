// مثال: إنشاء موقع ويب كامل
// Advanced Web Server Example

دالة تشغيل_الموقع() {
    متغير خادم = خادم_ويب(8080)
    
    // الصفحة الرئيسية
    دالة الرئيسية() {
        متغير html = "
        <html>
            <head>
                <title>موقعي العربي</title>
                <style>
                    body { font-family: Arial; direction: rtl; text-align: right; padding: 20px; }
                    h1 { color: #2196F3; }
                    .card { background: #f5f5f5; padding: 15px; margin: 10px 0; border-radius: 5px; }
                </style>
            </head>
            <body>
                <h1>🌐 أهلاً بك في موقعي العربي!</h1>
                <div class='card'>
                    <h2>مرحباً بك</h2>
                    <p>هذا موقع تجريبي تم إنشاؤه باللغة العربية!</p>
                </div>
                <div class='card'>
                    <h2>المميزات</h2>
                    <ul>
                        <li>دعم كامل للغة العربية</li>
                        <li>إنشاء مواقع بسهولة</li>
                        <li>قواعد بيانات مدمجة</li>
                    </ul>
                </div>
                <a href='/about'>من نحن</a> | 
                <a href='/api/data'>API بيانات</a>
            </body>
        </html>
        "
        ارجع html(html)
    }
    
    // صفحة من نحن
    دالة من_نحن() {
        متغير html = "
        <html>
            <head><title>من نحن</title></head>
            <body style='font-family: Arial; direction: rtl; padding: 20px;'>
                <h1>من نحن</h1>
                <p>نحن فريق مطور للغة البرمجة العربية!</p>
                <a href='/'>العودة للرئيسية</a>
            </body>
        </html>
        "
        ارجع html(html)
    }
    
    // API لإرجاع بيانات JSON
    دالة بيانات() {
        متغير بيانات = {
            "اسم": "موقع عربي",
            "اصدار": "1.0",
            "ميزات": ["دعم عربي", "قواعد بيانات", "API"],
            "تاريخ": وقت_الآن()
        }
        ارجع رد_جسون(بيانات)
    }
    
    اطبع_سطر "جاري تشغيل الموقع..."
    اطبع_سطر "افتح المتصفح على: http://localhost:8080"
    
    // ملاحظة: هذا الكود يحتاج لتشغيل الخادم
    // خادم.اربط('/', الرئيسية)
    // خادم.اربط('/about', من_نحن)
    // خادم.اربط('/api/data', بيانات)
    // خادم.شغل()
    
    اطبع_سطر "الموقع جاهز!"
}

تشغيل_الموقع()
