# 🌟 نواة - لغة البرمجة العربية المتقدمة
# Nawa Programming Language

<div dir="rtl">

## 🎯 عن نواة

**نواة** هي لغة برمجة عربية متكاملة تتيح لك بناء:
- 🌐 مواقع ويب كاملة
- 📱 تطبيقات سطح المكتب
- 💾 قواعد بيانات
- 🔌 واجهات API
- 📁 التعامل مع الملفات
- 🔐 التشفير والأمان

## ✨ المميزات

- **كلمات مفتاحية عربية** - كل شيء بالعربي!
- **سهلة التعلم** - بناء جملة بسيط وواضح
- **قوية** - دعم كامل للميزات المتقدمة
- **مجانية** - مفتوحة المصدر للجميع

## 📦 التثبيت

لا يحتاج تثبيت! فقط Python 3:

```bash
python3 nawa.py برنامج.nawa
```

## 🚀 البدء السريع

### مثال بسيط

```nawa
// أول برنامج لك
اطبع_سطر "أهلاً بك في نواة!"

// المتغيرات
متغير اسم = "أحمد"
متغير عمر = 25

اطبع_سطر "مرحباً "
اطبع_سطر اسم
```

### إنشاء موقع ويب

```nawa
// خادم ويب بسيط
دالة تشغيل_الموقع() {
    متغير خادم = خادم_ويب(8080)
    
    دالة الرئيسية() {
        متغير html = "<h1>مرحباً بك!</h1>"
        ارجع html(html)
    }
    
    خادم.رابط('/', الرئيسية)
    خادم.شغل()
}

تشغيل_الموقع()
```

### قاعدة بيانات

```nawa
// تطبيق مهام
متغير قاعدة = قاعدة_بيانات('tasks.db')

// إنشاء جدول
قاعدة.نفذ('CREATE TABLE IF NOT EXISTS tasks (id INTEGER, title TEXT)')

// إضافة بيانات
قاعدة.نفذ('INSERT INTO tasks VALUES (?, ?)', [1, 'تعلم نواة'])

// جلب بيانات
متغير مهام = قاعدة.نفذ('SELECT * FROM tasks')
لكل مهمة في مهام {
    اطبع_سطر مهمة
}
```

## 📚 الكلمات المفتاحية

### الأساسية
| الكلمة | الوظيفة |
|--------|---------|
| `اطبع` | طباعة بدون سطر جديد |
| `اطبع_سطر` | طباعة مع سطر جديد |
| `متغير` | تعريف متغير |
| `ثابت` | تعريف ثابت |
| `اذا` | شرط إذا |
| `والا` | شرط وإلا |
| `بينما` | حلقة بينما |
| `لكل` | حلقة لكل |
| `في` | في (لحلقات) |
| `دالة` | تعريف دالة |
| `ارجع` | إرجاع من دالة |

### المنطقية
- `صحيح` - True
- `خطأ` - False
- `فارغ` - Null
- `و` - AND
- `او` - OR
- `ليس` - NOT

## 📦 المكتبة القياسية

### الدوال الأساسية
```nawa
طول([1,2,3])        // 3
مدى(1, 6)           // [1,2,3,4,5]
مطلق(-5)             // 5
تقريب(3.14)          // 3
اقصى([1,5,3])        // 5
ادنى([1,5,3])        // 1
مجموع([1,2,3])       // 6
```

### الويب
```nawa
خادم_ويب(8080)       // إنشاء خادم
html("محتوى")        // استجابة HTML
رد_جسون({"م": 1})   // استجابة JSON
```

### قواعد البيانات
```nawa
قاعدة_بيانات('app.db')  // إنشاء قاعدة
قاعدة.نفذ(sql)          // تنفيذ استعلام
```

### الملفات
```nawa
اقرا_ملف('ملف.txt')     // قراءة ملف
اكتب_ملف('ملف.txt', 'نص')  // كتابة ملف
انشئ_مجلد('مجلد')       // إنشاء مجلد
احذف('ملف')             // حذف
```

### HTTP
```nawa
http_جلب('https://api.example.com')
http_ارسل('https://api.example.com', بيانات)
```

### التشفير
```nawa
هاش_مد5('نص')
هاش_شا256('نص')
هاش_شا512('نص')
```

### الوقت
```nawa
وقت_الآن()      // 2024-01-01T12:00:00
تاريخ_الآن()    // 2024-01-01
```

## 🎯 أمثلة شاملة

### 1. آلة حاسبة
```nawa
دالة جمع(أ, ب) {
    ارجع أ + ب
}

دالة ضرب(أ, ب) {
    ارجع أ * ب
}

اطبع_سطر جمع(10, 5)    // 15
اطبع_سطر ضرب(3, 4)     // 12
```

### 2. إدارة مستخدمين
```nawa
متغير قاعدة = قاعدة_بيانات('users.db')

قاعدة.نفذ('''
    CREATE TABLE IF NOT EXISTS users (
        id INTEGER PRIMARY KEY,
        name TEXT,
        email TEXT
    )
''')

دالة اضف_مستخدم(اسم, ايميل) {
    قاعدة.نفذ(
        'INSERT INTO users (name, email) VALUES (?, ?)',
        [اسم, ايميل]
    )
}

اضف_مستخدم("أحمد", "ahmed@example.com")
اضف_مستخدم("فاطمة", "fatima@example.com")

متغير مستخدمين = قاعدة.نفذ('SELECT * FROM users')
لكل مستخدم في مستخدمين {
    اطبع_سطر مستخدم
}
```

### 3. معالجة ملفات
```nawa
// قراءة وكتابة
متغير محتوى = اقرا_ملف('input.txt')
اكتب_ملف('output.txt', محتوى)

// تنظيم ملفات
انشئ_مجلد('backup')
انسخ('file.txt', 'backup/file.txt')

// قائمة الملفات
متغير ملفات = قائمة_ملفات('.')
لكل ملف في ملفات {
    اطبع_سطر ملف
}
```

## 🔧 الوضع التفاعلي

لتشغيل الوضع التفاعلي (REPL):

```bash
python3 nawa.py
```

ستظهر لك شاشة تفاعلية:
```
╔═══════════════════════════════════════════════════╗
║         نواة - لغة البرمجة العربية المتقدمة       ║
╚═══════════════════════════════════════════════════╝

نواة >>> متغير س = 10
نواة >>> متغير ص = 20
نواة >>> اطبع_سطر س + ص
30
نواة >>> خروج
```

## 📁 هيكلة المشروع

```
project/
├── nawa.py          # مترجم نواة
├── برنامج.nawa      # ملفك الرئيسي
├── examples/        # أمثلة
│   ├── hello.nawa
│   ├── web.nawa
│   └── db.nawa
└── README.md        # التوثيق
```

## 🎨 أفضل الممارسات

1. **استخدم أسماء واضحة**
```nawa
// جيد
متغير اسم_المستخدم = "أحمد"

// تجنب
متغير س = "أحمد"
```

2. **علّق كودك**
```nawa
// هذا دالة لحساب المجموع
دالة احسب_المجموع(أرقام) {
    ارجع مجموع(أرقام)
}
```

3. **نظّم كودك في دوال**
```nawa
دالة رئيسي() {
    دالة_1()
    دالة_2()
}

دالة دالة_1() {
    // ...
}
```

## 🐛 حل المشاكل

### خطأ: متغير غير معرف
تأكد من تعريف المتغير قبل استخدامه:
```nawa
متغير س = 10  // عرّف أولاً
اطبع_سطر س     // استخدم ثانياً
```

### خطأ: ملف غير موجود
تحقق من وجود الملف:
```nawa
اذا ملف_موجود('ملف.txt') {
    متغير محتوى = اقرا_ملف('ملف.txt')
}
والا {
    اطبع_سطر "الملف غير موجود"
}
```

## 📖 الإصدار

- **الإصدار الحالي**: 1.0.0
- **التاريخ**: 2024
- **الحالة**: مستقر

## 🤝 المساهمة

نواة مشروع مفتوح المصدر. مرحب بمساهماتكم!

## 📄 الترخيص

مجاني للاستخدام الشخصي والتعليمي والتجاري.

---

<div align="center">

**صنع بحب ❤️ باللغة العربية**

[نواة](nawa.py) - لغة المستقبل

</div>

</div>
