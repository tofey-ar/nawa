# 🌟 لغة نواة للبرمجة - Nawa Programming Language

<div dir="rtl">

## 📋 نظرة عامة

**نواة** هي لغة برمجة عربية متكاملة تم تطويرها من الصفر. تتيح لك بناء مواقع ويب، تطبيقات، قواعد بيانات، والتعامل مع الملفات باستخدام كلمات مفتاحية عربية بحتة.

## 🎯 المميزات الرئيسية

### 1. 🌐 تطوير الويب
- خادم ويب مدمج
- دعم HTML و JSON
- توجيه الروابط (Routing)

### 2. 💾 قواعد البيانات
- دعم SQLite كامل
- إنشاء الجداول
- إدراج، تحديث، حذف، واستعلام البيانات

### 3. 📁 نظام الملفات
- قراءة وكتابة الملفات
- إنشاء وحذف المجلدات
- نسخ ونقل الملفات

### 4. 🔐 الأمان والتشفير
- MD5, SHA256, SHA512
- تشفير كلمات المرور

### 5. 🌐 HTTP Client
- جلب البيانات من APIs
- إرسال طلبات POST

## 📂 هيكل المشروع

```
arabs/
├── nawa.py                    # مترجم نواة الرئيسي
├── README_NAWA.md             # التوثيق الكامل
├── examples/                  # أمثلة شاملة
│   ├── nawa_hello.nawa        # برنامج ترحيبي
│   ├── nawa_tasks.nawa        # تطبيق إدارة المهام
│   ├── nawa_files.nawa        # التعامل مع الملفات
│   └── nawa_crypto.nawa       # التشفير والأمان
└── هذا الملف                  # ملخص المشروع
```

## 🚀 البدء السريع

### تشغيل برنامج
```bash
python3 nawa.py examples/nawa_hello.nawa
```

### الوضع التفاعلي
```bash
python3 nawa.py
```

## 📝 أمثلة الكود

### برنامج بسيط
```nawa
اطبع_سطر "أهلاً بك في نواة!"
متغير اسم = "أحمد"
اطبع_سطر "مرحباً "
اطبع_سطر اسم
```

### دالة
```nawa
دالة جمع(أ, ب) {
    ارجع أ + ب
}

اطبع_سطر جمع(10, 5)  // 15
```

### قاعدة بيانات
```nawa
متغير db = قاعدة_بيانات('app.db')
db.نفذ('CREATE TABLE users (id INTEGER, name TEXT)')
db.نفذ('INSERT INTO users VALUES (?, ?)', [1, 'أحمد'])
متغير مستخدمين = db.نفذ('SELECT * FROM users')
```

### ملفات
```nawa
اكتب_ملف('ملف.txt', 'محتوى')
متغير محتوى = اقرا_ملف('ملف.txt')
انشئ_مجلد('مجلد جديد')
```

### تشفير
```nawa
متغير مشفر = هاش_شا256('كلمة سر')
```

## 🔑 الكلمات المفتاحية

### الأساسية
| الكلمة | الوظيفة |
|--------|---------|
| `اطبع` | طباعة بدون سطر جديد |
| `اطبع_سطر` | طباعة مع سطر جديد |
| `متغير` | تعريف متغير |
| `ثابت` | تعريف ثابت |
| `اذا` | شرط إذا |
| `والا` | شرط وإلا |
| `بينما` | حلقة بينما |
| `لكل` | حلقة لكل |
| `في` | في (لحلقات) |
| `دالة` | تعريف دالة |
| `ارجع` | إرجاع من دالة |

### المنطقية
- `صحيح` - True
- `خطأ` - False
- `فارغ` - Null
- `و` - AND
- `او` - OR
- `ليس` - NOT

## 📦 المكتبة القياسية

### الدوال الأساسية
```nawa
طول([1,2,3])        // 3
مدى(1, 6)           // [1,2,3,4,5]
مطلق(-5)             // 5
تقريب(3.14)          // 3
اقصى([1,5,3])        // 5
ادنى([1,5,3])        // 1
مجموع([1,2,3])       // 6
فرز([3,1,2])         // [1,2,3]
```

### الويب
```nawa
خادم_ويب(8080)       // إنشاء خادم ويب
html("محتوى")        // استجابة HTML
رد_جسون({"م": 1})   // استجابة JSON
```

### قواعد البيانات
```nawa
قاعدة_بيانات('app.db')  // إنشاء قاعدة
db.نفذ(sql, params)     // تنفيذ استعلام
```

### الملفات
```nawa
اقرا_ملف('ملف.txt')     // قراءة ملف
اكتب_ملف('ملف.txt', 'نص')  // كتابة ملف
الصق_ملف('ملف.txt', 'نص')  // إضافة للملف
ملف_موجود('ملف.txt')    // التحقق من الوجود
قائمة_ملفات('.')        // قائمة الملفات
انشئ_مجلد('مجلد')       // إنشاء مجلد
احذف('ملف')             // حذف ملف/مجلد
انسخ('من', 'إلى')       // نسخ ملف
انقل('من', 'إلى')       // نقل ملف
```

### HTTP
```nawa
http_جلب('https://api.example.com')
http_ارسل('https://api.example.com', بيانات)
```

### التشفير
```nawa
هاش_مد5('نص')
هاش_شا256('نص')
هاش_شا512('نص')
```

### الوقت
```nawa
وقت_الآن()      // 2024-01-01T12:00:00
تاريخ_الآن()    // 2024-01-01
```

### JSON
```nawa
الى_جسون({"م": 1})     // تحويل إلى JSON
من_جسون('{"م": 1}')   // تحويل من JSON
```

## 🧪 تشغيل الأمثلة

```bash
# البرنامج الترحيبي
python3 nawa.py examples/nawa_hello.nawa

# تطبيق إدارة المهام
python3 nawa.py examples/nawa_tasks.nawa

# التعامل مع الملفات
python3 nawa.py examples/nawa_files.nawa

# التشفير والأمان
python3 nawa.py examples/nawa_crypto.nawa
```

## 📊 مقارنة اللغات

| الميزة | نواة | Python | JavaScript |
|--------|------|--------|------------|
| كلمات عربية | ✅ | ❌ | ❌ |
| ويب | ✅ | ✅ | ✅ |
| قواعد بيانات | ✅ | ✅ | ✅ |
| ملفات | ✅ | ✅ | ✅ |
| سهولة | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

## 🎓 التعلم

### للمبتدئين
1. ابدأ بـ `nawa_hello.nawa`
2. تعلم المتغيرات والدوال
3. جرب الحلقات والشروط

### للمتقدمين
1. استكشف `nawa_tasks.nawa` لقواعد البيانات
2. جرب `nawa_files.nawa` للملفات
3. استخدم `nawa_crypto.nawa` للأمان

## 🐛 حل المشاكل

### خطأ: متغير غير معرف
```nawa
// خطأ
اطبع_سطر س  // س غير معرف

// صحيح
متغير س = 10
اطبع_سطر س
```

### خطأ: ملف غير موجود
```nawa
اذا ملف_موجود('ملف.txt') {
    متغير محتوى = اقرا_ملف('ملف.txt')
} والا {
    اطبع_سطر "الملف غير موجود"
}
```

## 📈 الإصدارات

### الإصدار 1.0.0 (الحالي)
- ✅ كلمات مفتاحية عربية
- ✅ دوال ومكتبات قياسية
- ✅ قواعد بيانات SQLite
- ✅ نظام ملفات
- ✅ تشفير وأمان
- ✅ HTTP Client
- ✅ خادم ويب

## 🔮 المستقبل

- [ ] دعم الفئات (Classes)
- [ ] واجهات رسومية (GUI)
- [ ] دعم أكبر للويب
- [ ] مكتبة حزم (Package Manager)

## 🤝 المساهمة

نواة مشروع مفتوح المصدر. مرحب بمساهماتكم!

## 📄 الترخيص

مجاني للاستخدام الشخصي والتعليمي والتجاري.

---

<div align="center">

**صنع بحب ❤️ باللغة العربية**

**نواة** - لغة البرمجة العربية المتقدمة

الإصدار: 1.0.0 | 2024

</div>

</div>
